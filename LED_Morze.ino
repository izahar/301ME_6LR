#define LED_A 13
void setup() {
 pinMode(LED_A, OUTPUT);

void LED_ Semubalamyt ( ) {
 digitalWrite (LED_A, HIGH);
 delay (500);
 digitalWrite (LED_A, LOW);
 delay (500);

 digitalWrite (LED_A, HIGH);
 delay (500);
 digitalWrite (LED_A, LOW);
 delay (500);

 digitalWrite (LED_A, HIGH);
 delay (500);
 digitalWrite (LED_A, LOW);
 delay (500);
}
